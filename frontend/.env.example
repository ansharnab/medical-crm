# Backend API origin (no trailing slash).
# Must match backend PORT in backend/.env (default 4000).
VITE_API_URL=http://localhost:4001

# Used by Vite dev proxy when requests use relative /api paths.
# Keep in sync with backend PORT.
VITE_PROXY_TARGET=http://localhost:4001
